# ─────────────────────────────────────────────────────────────────────────────
# Regime-Switching Monte Carlo Simulation — Python dependencies
# Install with:  pip install -r requirements.txt
# Python >= 3.10 required
# ─────────────────────────────────────────────────────────────────────────────

# ── Core numerical stack ──────────────────────────────────────────────────────
numpy>=1.26,<2.0
pandas>=2.1,<3.0
scipy>=1.12,<2.0

# ── Deep learning (PyTorch — CPU build; swap index URL for GPU) ───────────────
# For CUDA 12.x:  pip install torch --index-url https://download.pytorch.org/whl/cu121
torch>=2.2,<3.0

# ── Transformers / FinBERT sentiment ─────────────────────────────────────────
transformers>=4.40,<5.0
accelerate>=0.29,<2.0        # required by transformers for device-aware loading

# ── Hidden Markov Models ──────────────────────────────────────────────────────
hmmlearn>=0.3,<1.0
scikit-learn>=1.4,<2.0       # hmmlearn runtime dependency

# ── Financial network analysis ────────────────────────────────────────────────
networkx>=3.2,<4.0

# ── Market data ───────────────────────────────────────────────────────────────
yfinance>=0.2.38

# ── Visualisation ────────────────────────────────────────────────────────────
matplotlib>=3.8,<4.0

# ── Testing ───────────────────────────────────────────────────────────────────
pytest>=8.0,<9.0
